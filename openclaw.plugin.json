{
  "id": "open-second-brain",
  "name": "Open Second Brain",
  "description": "Second brain for AI agents using Obsidian-compatible Markdown vaults. Provides vault management, event logging, wiki indexing, and an MCP tool server.",
  "version": "0.5.0",
  "contracts": {
    "tools": [
      "second_brain_status",
      "second_brain_query",
      "second_brain_capture",
      "event_log_append",
      "vault_health"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "vault": {
        "type": "string",
        "description": "Absolute path to the Obsidian-compatible vault directory."
      },
      "instanceName": {
        "type": "string",
        "description": "Human-readable name for this second brain instance."
      },
      "mcpEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Whether to enable the MCP tool server."
      }
    }
  }
}
